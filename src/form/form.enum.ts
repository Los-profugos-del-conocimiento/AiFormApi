export enum Type {
    surbey = 'survey',
    quiz = 'quiz'
}

export enum ResponseType {
    radio = 'radio',
    checkbox = 'checkbox',
    dropdown = 'dropdown',
    text = 'text',
    scale = 'scale',
    date = 'date',
    time = 'time',
    fileUpload = 'fileUpload',
    row = 'row'
}
