export enum Type {
    survey = 'survey',
    quiz = 'quiz'
}

export enum ResponseType {
    multipleChoice = 'multipleChoice',
    text = 'text',
    scale = 'scale',
    date = 'date',
    time = 'time',
    fileUpload = 'fileUpload',
    row = 'row'
}
